# Base URL for the Patient Portal API. This should point to the backend service.
# Example: https://api.example.com or http://localhost:3000
NEXT_PUBLIC_PATIENT_PORTAL_API_BASE_URL=

# Non-public fallback for server-side calls if different from the public URL.
PATIENT_PORTAL_API_BASE_URL=
